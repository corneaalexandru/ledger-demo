# Ledger Public uses a user-owned Google Sheet.
LEDGER_STORE=google

# The spreadsheet ID from the Google Sheet URL.
# Example URL:
# https://docs.google.com/spreadsheets/d/SPREADSHEET_ID_HERE/edit
LEDGER_SPREADSHEET_ID=your_google_sheet_id_here

# Path to your own service-account JSON file.
# This file is ignored by Git and must never be committed.
GOOGLE_APPLICATION_CREDENTIALS=./credentials/ledger-service-account.json

# Default project/display currency. Users can also switch it later in Settings.
# Supported: EUR, USD, AED, RON, GBP, CHF, CAD, AUD, INR, JPY.
LEDGER_PROJECT_CURRENCY=EUR

# Optional: local browser password config. start_ledger_public.command creates
# .ledger_auth.json once. Set this to 1 only for trusted local development.
# LEDGER_AUTH_DISABLED=0
